name: "Jest code coverage commenter"
description: "Post code coverage generated by jest (istanbul) to Github"
inputs:
    github-token:
        description: "A valid github access token for the repo. Should automaticly be available via secrets.GITHUB_TOKEN"

runs:
    using: "node12"
    main: "index.js"
branding:
    icon: "zap"
    color: "blue"
