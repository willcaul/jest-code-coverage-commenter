{
  "name": "jest-code-coverage-commenter",
  "version": "1.0.0",
  "description": "Post code coverage generated by jest (istanbul) to Github",
  "main": "dist/index.js",
  "scripts": {
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "author": "firmnav",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@actions/github": "^3.0.0",
    "@types/istanbul-lib-coverage": "^2.0.3",
    "istanbul-lib-coverage": "^3.0.0",
    "markdown-table": "^2.0.0"
  },
  "devDependencies": {
    "@types/markdown-table": "^2.0.0",
    "@types/node": "^12.12.8",
    "typescript": "^3.7.2"
  }
}
